# Git for Writers: คู่มือสำหรับนักเขียน

Git เปรียบเสมือน "Time Machine" สำหรับงานเขียนของคุณ ช่วยให้คุณบันทึกประวัติการแก้ไข ย้อนกลับไปดูเวอร์ชันเก่า และทดลองไอเดียใหม่ๆ ได้อย่างอิสระ

## Concept สำคัญ (เปรียบเทียบกับงานเขียน)

1.  **Repository (Repo):** เหมือน "แฟ้มงาน" หรือ "โฟลเดอร์โปรเจกต์" ที่เก็บรวบรวมไฟล์งานเขียนทั้งหมดและประวัติการแก้ไข
2.  **Commit:** เหมือนการกด "Save" แต่เจ๋งกว่าตรงที่มันจะบันทึกว่า "แก้ไขอะไรไป" และ "เมื่อไหร่" (Snapshot)
3.  **Branch:** เหมือน "โลกคู่ขนาน" หรือ "ฉบับร่างทางเลือก" เช่น แตก Branch ไปเขียนตอนจบแบบเศร้า โดยไม่กระทบฉบับร่างหลัก
4.  **Merge:** การเอาเนื้อหาจาก Branch หนึ่ง มารวมกับร่างหลัก

---

## 🛠 คำสั่งพื้นฐานที่ใช้บ่อย (Basic Commands)

### 1. เริ่มต้นโปรเจกต์ใหม่ (`git init`)
ใช้ครั้งแรกสุดเมื่อสร้างโฟลเดอร์งานเขียน

```bash
git init
```
*   **เมื่อไหร่:** เมื่อสร้าง Folder ใหม่
*   **ผลลัพธ์:** สร้างระบบ Git ขึ้นมาใน Folder นั้น

### 2. เช็คสถานะ (`git status`)
ดูว่ามีไฟล์ไหนแก้ไขไปบ้าง หรือไฟล์ไหนยังไม่ได้ Save (Commit)

```bash
git status
```
*   **สำคัญมาก!** ควรพิมพ์บ่อยๆ เพื่อเช็คว่าเรากำลังทำอะไรอยู่

### 3. เตรียมไฟล์เพื่อบันทึก (`git add`)
เหมือนการเลือกไฟล์งานที่จะ Save ลงกล่อง

```bash
git add ชื่อไฟล์.md
# หรือถ้าจะเอาทุกไฟล์
git add .
```

### 4. บันทึกงาน (`git commit`)
เหมือนกด Save พร้อมแปะโน๊ตกันลืมว่าแก้อะไรไป

```bash
git commit -m "เขียนบทนำเสร็จแล้ว"
```
*   `-m "..."` คือข้อความช่วยจำ (Message) ควรสื่อความหมายชัดเจน

### 5. ดูประวัติ (`git log`)
ดูว่าเรา Save อะไรไปบ้าง เมื่อไหร่

```bash
git log
# ถ้าอยากดูแบบย่อๆ บรรทัดเดียว
git log --oneline
```

### 6. ดูความเปลี่ยนแปลง (`git diff`)
ดูว่าสิ่งที่เราแก้ ล่าสุด ต่างจากที่ Save ไว้ยังไง

```bash
git diff
```

---

## ☁️ การทำงานกับ GitHub (Cloud)

เมื่อเราอยากเก็บงานไว้บนเน็ต หรือส่งงานให้บ.ก.

### 1. เชื่อมต่อกับ GitHub (ทำครั้งเดียว)
```bash
git remote add origin https://github.com/ggnniimm/writing-project.git
```

### 2. อัปโหลดงานขึ้นบน Cloud (`git push`)
หลังจาก `commit` ในเครื่องแล้ว ต้อง `push` เพื่อส่งขึ้นเน็ต

```bash
git push -u origin main
```
*   ใช้ครั้งแรกต้องมี `-u origin main` ต่อไปพิมพ์แค่ `git push` ก็พอ

### 3. ดึงงานล่าสุดจาก Cloud (`git pull`)
ถ้าไปเขียนใน iPad หรือเครื่องอื่น แล้วอยากดึงงานล่าสุดกลับมาทำต่อในคอม

```bash
git pull
```
---

## 💻 กรณีเปลี่ยนเครื่อง (เช่น ไปใช้ MacBook Air)

ถ้าจะย้ายไปเขียนเครื่องอื่น **ต้องทำเหมือนตอนเริ่มใหม่แค่บางขั้นตอน**:

1.  **ติดตั้ง Git** (ถ้ายังไม่มี)
2.  **ตั้งค่าชื่อ/อีเมล**:
    ```bash
    git config --global user.name "Mingsak Saengwilaipon"
    git config --global user.email "mmingssang@gmail.com"
    ```
3.  **สร้างกุญแจ (SSH Key) *คนละดอกกับเครื่องแรก*:**
    *   ทำเหมือนเดิม: `ssh-keygen ...`
    *   เอา Key ใหม่ที่ได้จากเครื่อง Air ไปแปะใน GitHub Settings (ตั้งชื่อว่า "MacBook Air")
4.  **ดึงงานมาทั้งก้อน (Clone):**
    *   ใช้คำสั่งนี้ (ทำครั้งแรกครั้งเดียว):
    ```bash
    git clone git@github.com:ggnniimm/writing-project.git
    ```
    *   จะได้ Folder `writing-project` มาทั้งก้อนเลย

5.  **เริ่มเขียนต่อได้เลย!** อย่าลืม `git pull` ก่อนเริ่มงานทุกครั้งเผื่อเครื่องเก่าแก้อะไรไว้

---

## 💡 Workflow การทำงานแนะนำ

1.  **เขียนงาน** -> พิมพ์ๆ แก้ๆ ในไฟล์
2.  **เช็ค** -> `git status` / `git diff` ดูว่าแก้อะไรไป
3.  **เตรียม** -> `git add .`
4.  **บันทึก** -> `git commit -m "อัปเดตย่อหน้าที่ 2"`
5.  **วนกลับไปข้อ 1**

---

## เคล็ดลับเพิ่มเติม
*   ควรตั้งชื่อไฟล์เป็นภาษาอังกฤษ และไม่เว้นวรรค (ใช้ `_` หรือ `-` แทน) เช่น `my_story_chap1.md`
*   **Markdown** เป็นเพื่อนที่ดีที่สุดของ Git เพราะมันเป็น Text ล้วนๆ ทำให้ Git เปรียบเทียบไฟล์ได้แม่นยำที่สุด
