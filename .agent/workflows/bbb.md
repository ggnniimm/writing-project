---
description: Start of day routine (Pull, Read README, Check Diary/Ideas)
---

// turbo
1. Run `git pull` to ensure the repository is up to date.
2. Read `README.md` **(CRITICAL: Do this every time)** to refresh your memory on project guidelines, file structure, and workflow commands.
3. Read the last 50 lines of `git_diary.md` to see the most recent activities and pending tasks.
4. Read `content_ideas.md` (or list the top available topics) to identify potential writing tasks for the day.
5. Check if an entry for **today's date** exists in `git_diary.md`.
    - If NO: Create a new entry using the standard template (see `README.md` or previous entries).
    - If YES: Read it to see what has already been logged.
6. Present a concise summary of the project status (Pending Tasks, Ready-to-Write Ideas) and ask the user for instructions.
